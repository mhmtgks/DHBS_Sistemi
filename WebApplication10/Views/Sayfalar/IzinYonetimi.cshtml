@using Service;
@{
    Layout = "~/Views/Shared/_Layout_Yonetici.cshtml";
    ViewBag.Title = "İzin Yöneticisi";
}



@if (TempData["AlertMessage"] != null)
{
    if (TempData["AlertMessage"] == "İzin ekleme Başarısız")
    {

        <body onload="failed()">
        </body>
    }
    else
    {
        <body onload="success()">
        </body>

    }


}


    <table id="tablo1" class="table table-striped table-bordered dt-resposive nowrap" style="">
        <thead>
            <tr>
                <th class="th-sm">
                    Çalışan Adı
                </th>
                <th class="th-sm">
                    Uzmanlık
                </th>
                <th class="th-sm">
                    Eski İzin Aralığı
                </th>
                <th class="th-sm">
                  İzin Aralığı  Girin
                </th>

            </tr>
        </thead>
        <tbody>
            @{
                YillikIzin maas = new YillikIzin();
            }
            @foreach (var item in maas.Lists("select distinct y.DoktorID,y.BaslangicTarihi,y.BitisTarihi,d.AdiSoyadi,d.Uzmanlık from YillikIzin  y join vw_doktorlar d on d.DoktorID=y.DoktorID"))
            {
                <tr>
                    <td>
                        @Html.Label(item.AdiSoyadi.ToString())
                    </td>
                    <td>
                        @Html.Label(item.Uzmanlık.ToString())
                    </td>
                    <td>
                        <label> @item.BaslangicTarihi.ToString("yyyy/MM/dd") - @item.BitisTarihi.ToString("yyyy/MM/dd")  </label>
                    </td>
                    <td>
                        @using (Html.BeginForm("IzinYonetimi", "Sayfalar", FormMethod.Post))
                        {
                        <input type="date" required="" name="BaslangicTarihi">
                            <input type="date" required="" name="BitisTarihi">
                        <button class="btn btn-success" name="DoktorID" value="@item.DoktorID">İzin Ekle</button>
                        }
                    </td>
               </tr>

            }

        </tbody>
    </table>

<style>
    /* Custom CSS to move the search bar to the right */
    div.dataTables_wrapper div.dataTables_filter {
        margin-left: 480px;
        margin-top: -150px;
    }

    div.dataTables_wrapper div.dataTables_length {
        margin-left: 90px;
        margin-top: 10px;
        visibility: hidden;
    }

    div.dataTables_info {
        visibility: hidden;
    }

</style>
